# Gestiolibra — frontend

SPA en React + TypeScript + Vite. MVP: login + agenda/turnos. Ver
`DECISIONS.md` (ADR-019) y el `README.md` de la raíz del repo (sección
"Frontend") para el diseño completo y cómo correr esto junto al
backend — no se repite acá.

```bash
npm install
npm run dev
```
